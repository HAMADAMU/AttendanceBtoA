<% provide(:title, "Top") %>
<div class="center jumbotron">
  <h1>勤怠システム</h1>
  <p>
    <% if logged_in? %>
      <%= link_to "ログイン", login_path, class: "btn btn-primary disabled" %>
    <% else %>
     <%= link_to "ログイン", login_path, class: "btn btn-primary" %>
    <% end %>
  </p>
  
  <p>
    <% if logged_in? %>
      アカウントの作成は<%= link_to "こちら" %>から
    <% else %>
      アカウントの作成は<%= link_to "こちら", new_user_path %>から
    <% end %>
  </p>
</div>

